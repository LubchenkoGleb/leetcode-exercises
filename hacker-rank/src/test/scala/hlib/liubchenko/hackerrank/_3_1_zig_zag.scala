package hlib.liubchenko.hackerrank

//package hlib.liubchenko.hackerrank
//
//object _3_1_zig_zag extends  extends AnyWordSpec with Matchers {
////  val lines = io.Source.stdin.getLines
////  val testCases = lines.next().toInt
////  (0 until testCases).foreach { i =>
////    val s = lines.next().toInt
////    val arr = lines.next().split(" ").map(_.toInt).sortInPlace()
////    val m = s / 2
////
////    println((arr.take(m) ++ arr.slice(m, arr.size).reverse).mkString(" "))
//
//    val c = 'c'
//    val r: Char = (c.toInt + 3).toChar
//    println(r)
//
//}
