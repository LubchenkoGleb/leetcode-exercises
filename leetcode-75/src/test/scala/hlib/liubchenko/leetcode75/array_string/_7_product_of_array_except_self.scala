package hlib.liubchenko.leetcode75.array_string

import org.scalatest.matchers.should.Matchers
import org.scalatest.wordspec.AnyWordSpec

class _7_product_of_array_except_self extends AnyWordSpec with Matchers {
//  def productExceptSelf(nums: Array[Int]): Array[Int] = {
//    val n = nums.length
//    val res = new Array[Int](nums.length)
//    res(n - 1) = nums(n - 1)
//    for (i <- n - 2 to 1 by -1) res(i) = res(i - 1) * nums(i)
//
//    res(0) = nums(0)
//    for(i <- 1 until n) res(i) =
//  }
}
